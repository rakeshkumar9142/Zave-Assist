# Appwrite (optional — falls back to in-memory seed data)
APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=
APPWRITE_API_KEY=
APPWRITE_DATABASE_ID=zave_assist

# Gemini (only used for product recommendations)
GEMINI_API_KEY=

# CORS
FRONTEND_URL=http://localhost:3000
